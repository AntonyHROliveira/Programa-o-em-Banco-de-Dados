CREATE DATABASE criandoDB;
USE criandoBD;

CREATE TABLE clientes(
    ID_Cliente INT PRIMARY KEY,
    Nome VARCHAR(50),
    Cidade VARCHAR(50),
    UF CHAR(2),
    Data_Cadastro DATE
);

CREATE TABLE categorias(
    ID_Categoria INT PRIMARY KEY,
    Nome_Categoria VARCHAR(50)
);

CREATE TABLE produtos(
    ID_Produto INT PRIMARY KEY,
    Descricao VARCHAR(25),
    Preco_unitario FLOAT,
    Estoque INT,
    FK_Categoria INT,
    Foreign KEY (FK_Categoria) REFERENCES categorias(ID_Categoria)
);

CREATE TABLE vendas (
    ID_Venda INT PRIMARY KEY,
    FK_Cliente INT,
    FK_Produto INT,
    Quantidade INT,
    Data_Vendas DATE,
    Foreign KEY (FK_Cliente) REFERENCES clientes(ID_Cliente),
    Foreign KEY (FK_Produto) REFERENCES produtos(ID_Produto)
);


USE criandoDB;
INSERT INTO categorias VALUES(1,'Eletronicos');
INSERT INTO categorias VALUES(2,'Moveis');

INSERT INTO produtos VALUES(101,'Smartphone X', 2500.00,50,1);
INSERT INTO produtos VALUES(102,'Cadeira Gamer', 2500.00,50,1);
INSERT INTO produtos VALUES(103,'Mouse Sem Fio', 2500.00,50,1);
INSERT INTO produtos VALUES(104,'Monitor 4K', 2500.00,50,1);
INSERT INTO produtos VALUES(105,'Mesa de Escritorio', 2500.00,50,1);

INSERT INTO clientes VALUES(10,'Ana Silva','São Paulo','SP','2023-01-15');
INSERT INTO clientes VALUES(10,'Bruno Souza','Curitiba','PR','2023-05-20');
INSERT INTO clientes VALUES(10,'Carlas Dias','São Paulo','SP','2024-02-10');
INSERT INTO clientes VALUES(10,'Diego Lemos','Belo Horizonte','MG','2024-03-01');
 

















